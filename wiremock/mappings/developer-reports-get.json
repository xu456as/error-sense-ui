{
  "request": {
    "method": "GET",
    "urlPath": "/api/developer-reports"
  },
  "response": {
    "status": 200,
    "headers": {
      "Content-Type": "application/json",
      "Access-Control-Allow-Origin": "*"
    },
    "jsonBody": {
      "items": [
        {
          "id": 1,
          "errorPattern": "NullPointerException at CheckoutPricingService",
          "appearTimes": 42,
          "rootCauseAnalysis": "Pricing payload omits discount config when fallback rules are empty.",
          "suggestion": "Validate promotion rules before calculating totals and add guard rails in mapper.",
          "criticalLevel": "Critical"
        },
        {
          "id": 2,
          "errorPattern": "Timeout while calling customer-preference API",
          "appearTimes": 17,
          "rootCauseAnalysis": "Retry chain saturates the thread pool during large batch replay jobs.",
          "suggestion": "Reduce retry fan-out and move replay traffic onto a separate worker queue.",
          "criticalLevel": "High"
        },
        {
          "id": 3,
          "errorPattern": "Duplicate key violation on order_audit table",
          "appearTimes": 8,
          "rootCauseAnalysis": "Idempotency key is generated after event retries and changes across attempts.",
          "suggestion": "Persist the original correlation id and reuse it for audit write deduplication.",
          "criticalLevel": "Medium"
        }
      ]
    }
  }
}
